Мелкие замечания: в `ReportMakerHelper` методы помечены как статические через `$`, что не является стандартным синтаксисом Mermaid, но допустимо для обозначения логики.

⚠️ SUSPICIOUS: avdeeva_anfisa (структура классов, интерфейсов и методов ReportMakerHelper практически идентична)