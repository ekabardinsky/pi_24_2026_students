Мелкие замечания: в методах билдеров `AddNode`, `AddEdge` и `Build` указаны ключевые слова `override`, что предполагает наличие этих методов в базовом классе `GraphBuilder`, однако в описании `GraphBuilder` они не объявлены как `virtual`.