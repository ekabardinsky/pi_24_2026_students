1. Отсутствует разделение ответственности на отдельные классы для оформления (Formatter) и вычисления показателей (Calculator), как того требует задание (вместо делегирования использован вспомогательный класс с набором функций).
2. Не реализовано делегирование через интерфейсы или абстрактные классы, вместо этого используется передача `Func` в конструктор, что не решает проблему расширяемости оформления и логики по отдельности.

Мелкие замечания: использование `Func` в полях класса вместо полноценных объектов-делегатов.